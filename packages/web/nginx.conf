server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 📖 学习点：SPA 路由处理
    # 所有未匹配到静态文件的请求都返回 index.html
    # 这样前端路由（如 React Router）才能正常工作
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 📖 静态资源缓存
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
