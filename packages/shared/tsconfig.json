// 📖 ==========================================
// 📖 shared/tsconfig.json 配置说明
// 📖 ==========================================
// 共享类型包的 TypeScript 配置。
// 这个包专门存放前后端共用的类型定义（interface、type）。
// 前后端都引用这个包，就能保证数据类型一致。
{
    // 继承根目录的共享 TS 配置
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        // 编译输出到 dist/ 目录（虽然类型包通常不需要编译，但保留以防万一）
        "outDir": "./dist",
        // 源码在 src/ 目录
        "rootDir": "./src"
    },
    // 只编译 src/ 下的文件
    "include": [
        "src/**/*"
    ]
}