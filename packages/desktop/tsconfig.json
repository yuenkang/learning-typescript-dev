// 📖 ==========================================
// 📖 tsconfig.json — 桌面应用的 TS 项目引用配置
// 📖 ==========================================
// 和 client 一样，使用 Project References 分离不同环境的配置：
// - tsconfig.app.json → 渲染进程（React 代码，运行在浏览器环境）
// - tsconfig.node.json → 主进程 + preload（运行在 Node.js 环境）
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
