{
  "name": "bookmark-manager",
  "version": "1.0.0",
  "private": true,
  "description": "书签管理器 - TypeScript 全栈学习项目",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/server & npm run dev --workspace=packages/web",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web",
    "dev:desktop": "npm run dev --workspace=packages/desktop",
    "dev:tui": "npm run dev --workspace=packages/tui",
    "build:tui": "npm run build --workspace=packages/tui",
    "build:desktop": "npm run pack --workspace=packages/desktop",
    "dist:desktop": "npm run dist --workspace=packages/desktop"
  }
}
